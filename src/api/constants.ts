
export enum APISTATUS {
    SUCCESS = 'SUCCESS',
    FAILED = 'FAILED'
}

export enum APICODE {
    SUCCESS = 200,
    INTERNAL_SERVER = 500
}

export enum FETCHSTATUS {
    SUCCESS = 'SUCCESS',
    PROGRESS = 'PROGRESS',
    ERROR = 'ERROR'
}
